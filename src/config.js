'use strict';

const config = {};

/**
 *  App name for logging etc.
 */
config.app = 'serverless-apigateway-route-settings';

/**
 * Key in serverless.yml where users will store the Route Settings Data.
 */
config.key = 'httpApiRouteSettings';

module.exports = config;
